{
  "name": "tag-action",
  "version": "1.0.0",
  "description": "Modern SemVer tagging for GitHub Actions. Robust, fast, and configurable version management with zero external dependencies.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "ncc build src/index.js -o dist --minify --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Ananta Cloud",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.1.0",
    "semver": "^7.7.4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
